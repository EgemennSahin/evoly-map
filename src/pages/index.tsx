import MapView from "@/components/MapView";

const HomePage = () => {
  return (
    <div>
      <h1>Map with points on coordinates</h1>
      <MapView />
    </div>
  );
};

export default HomePage;
